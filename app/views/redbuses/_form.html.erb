<%= form_with(model: redbus, local: true) do |form| %>
  <% if redbus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(redbus.errors.count, "error") %> prohibited this redbus from being saved:</h2>

      <ul>
        <% redbus.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :account %>
    <%= form.number_field :account %>
  </div>

  <div class="field">
    <%= form.label :offers %>
    <%= form.text_field :offers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
